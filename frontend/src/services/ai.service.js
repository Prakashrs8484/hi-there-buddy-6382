// Placeholder for AI integration (OpenAI)
// Example: create functions to format user context and call OpenAI
module.exports = {
  generateFinanceReply: async (context, message) => {
    // Integrate with OpenAI here
    return `Echo: ${message}`;
  }
};
